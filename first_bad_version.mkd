```c
bool isBadVersion(int version);

int firstBadVersion(int n) {
    int right=n,left=1,mid;
    while(left<right)
    {
        mid=left+(right-left)/2;
        if(isBadVersion(mid))
            right=mid;
        else left=mid+1;
    }
    return left;
}
```