```cpp
class Solution {
public:
    int climbStairs(int n) {
        if(n==1) return 1;
        vector<int>sum(n+2);
        sum[1]=1;
        sum[2]=2;
        int i;
        for(i=3;i<=n;i++)
        {
            sum[i]=sum[i-1]+sum[i-2];
        }
        return sum[i-1];
    }
};
```